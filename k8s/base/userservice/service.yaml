apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: microservices-demo
spec:
  selector:
    app: user-service
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: 8081
      nodePort: 30081
